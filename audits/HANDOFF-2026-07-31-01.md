# Remediation Brief 2026-07-31-01 — FINAL READ — for SDET / Dev / BA

**From:** independent-auditor (outside your loop). **Audited commit:** `0ef9f8d`.
**Full evidence:** `audits/AUDIT-2026-07-31-01.md` (read only if you want the proof).
**Overall audit verdict:** **PASS-WITH-CONCERNS** (0 blocker, 0 major, 3 minor).
**Go / no-go on the live bolus calculator:** **GO** — safe to trust as built, provided the
operator knowingly carries the ICR-correctness dependency (R1 below). The INV-1/Gate-2
retirement is **LEGITIMATE** (operator-authorised, clinically moot rather than bypassed, and
backstopped by INV-3/INV-4/`ValueError` + the audited profile).

## How to use this
Each item is: what to change · where · why it matters for her · **Done when** (the check that
closes it). There are **no blockers** — nothing here must be fixed before she uses the
calculator. Items are doc-hygiene and test-reliability only. If any item seems to ask you to
weaken a gate or an invariant, STOP and escalate — you have misread it.

## Priority order
1. H-F1 (BA) — correct the S-901 story doc to describe the *retired* gate
2. H-F2 (BA) — reconcile the S-901 / S-703 traceability test-columns with the INV-1 table
3. H-F3 (SDET) — de-flake the double-tap E2E under full-suite load

---

## For BA

### H-F1 — The S-901 story doc still describes the LIVE bolus calculator as gated on INV-1/Gate 2
- **Do:** Update `docs/stories/S-901.md` so it describes the calculator **as it now ships**: no
  `require_gate2(gate2_status(icr=icr))`, no "gated on a confirmed ICR (INV-1)", no "INV-1 first /
  INV-1 no bypass ⇒ `GateNotPassed`". Replace with the S-1011 reality — ICR is a required input;
  a missing/`<=0` ICR raises a plain `ValueError` (not a `SafetyViolation`/`GateNotPassed`);
  INV-3 and INV-4 are what bound the dose. Add a one-line pointer to S-1011/DL-035. Leave S-703.md
  and S-104.md as historical records **but** add a one-line "INV-1 later retired — see S-1011"
  note at their top so a reader is not misled that the gate is current.
- **Where:** `docs/stories/S-901.md:5,18,26,42,63,85` (the INV-1/Gate-2 narration); optional one-liners atop `docs/stories/S-703.md` and `docs/stories/S-104.md`.
- **Why (for her):** S-901.md is the human "what protects this dose" record for the code that
  now doses her; a doc claiming a confirmation gate the code no longer has is exactly the
  looks-done drift that lets a missing safeguard pass unnoticed.
- **Done when:** `grep -nE "require_gate2|gate2_status|gated on a confirmed ICR|INV-1 first" docs/stories/S-901.md` returns nothing, and the doc names the `ValueError`/INV-3/INV-4 backstop instead. Land it in a lone `docs(...)` BA commit (DL-030).

### H-F2 — Traceability test-columns for S-901 and S-703 still list the retired INV-1 behaviour
- **Do:** In the S-901 row (`docs/traceability.md:53`) and the S-703 row (`traceability.md:46`),
  amend the *test-description* text that reads "★ INV-1 icr=null ⇒ `GateNotPassed`, no bypass" to
  reflect S-1011 (icr=null/≤0 ⇒ `ValueError`, discrimination test asserts it is neither a
  `SafetyViolation` nor a `GateNotPassed`). The authoritative invariant table (`traceability.md:85`)
  is already correct — this is only about making the two halves agree.
- **Where:** `docs/traceability.md:46,53`.
- **Why (for her):** the matrix is the one instrument that catches a requirement silently losing
  its cover; when its rows disagree with its own invariant table, that instrument stops being
  trustworthy.
- **Done when:** the S-901 and S-703 rows no longer assert a live `GateNotPassed`-on-null-ICR for
  the dose path, and reading the matrix top-to-bottom gives one consistent story about INV-1.
  Lone `docs(...)` BA commit.

---

## For SDET

### H-F3 — De-flake the duplicate-bolus E2E under full-suite load
- **Do:** Make `tests/e2e/test_double_tap.py::test_a_lost_response_is_safe_to_retry`
  deterministic when the *entire* suite runs together. It passes 5/5 in its file, 36/36 in
  `tests/e2e/` alone, and 1/1 in isolation, but fails once in the full ~850-test run with a
  transient save-failure toast (`"Could not save — your entry is kept; try again."`) — a
  resource/timing contention symptom (multiple live servers + PyMC compilation), not an
  idempotency-logic bug. Fix the isolation/timing (e.g. serialise the E2E marker, give the live
  server its own DB/port per session as DL-051 did for a11y, or add a bounded retry on the *save
  round-trip* the test depends on — **not** on the assertion). Do **not** weaken `assert_saved`
  (DL-056) — it did its job by catching this.
- **Where:** `tests/e2e/test_double_tap.py`, `tests/e2e/conftest.py` (live_server / db fixtures).
- **Why (for her):** this is the guard against a *doubled bolus* — ~5 h of inflated IOB that
  under-doses her and corrupts a model feature (DL-055). A test that goes red for reasons unrelated
  to that logic trains reviewers to wave a red away, which is how a real red here would slip.
- **Done when:** the full suite (`pytest`, no `--ignore`) is green on two consecutive runs, or the
  E2E suite is documented as a separately-gated CI job that is deterministically green — and the
  double-tap logic assertion is unchanged.

---

## Items I explicitly did NOT raise (so you don't invent work)
- **The INV-1 / Gate-2 retirement itself.** It is legitimate — operator-authorised (DL-035),
  the confirmed-ICR condition was met before removal (DL-032), and the dose is still bounded by
  INV-3/INV-4 + the `ValueError`. **Do NOT re-introduce a dose gate** under any name; a bad ICR
  is an input error, not a `SafetyViolation`. The discrimination test that pins this must stay.
- **`recommend_bolus` raising a plain `ValueError`** on a missing/`<=0` ICR is CORRECT and must
  stay a `ValueError` (not upgraded to a `SafetyViolation`).
- **INV-2..9 as one-function-per-invariant in `core/safety.py`**, `GateNotPassed` retained for
  Gate 1, zero project imports — all correct. Do not "tidy" or consolidate.
- **INV-7's flag ∪ ledger sourcing** (`data/repositories.py`) and **INV-8's two call sites**
  (`models/params.py`, `models/bayesian_ordinal.py`) are correct; do not collapse them.
- **The Gate-1 promotion-only-via-Python gap and the `is_promoted`-in-`is_open` design** are
  intentional and fail *safe*; the `is_open`-vs-`can_be_promoted` split in `prescribe/gates.py`
  is deliberate. Do not "fix" it by gating promotion on `is_open`.
- **`datetime.now()` in `core/clock.py`, `data/provenance.py`, and the comment in
  `api/schemas.py`** are sanctioned non-clinical uses (ADR-8). Do not touch them.
- **Historical story docs S-104/S-703 describing INV-1 as active** need only a one-line "later
  retired" note (H-F1), not rewriting — they are point-in-time records.

## Residual risks the operator should carry knowingly (route to the operator; do not "fix" in code)
- **R1 — ICR-correctness dependency.** Retiring Gate 2 means a *plausible-but-wrong positive ICR*
  in the profile will silently mis-dose her whenever the dose stays under 15 U. Backstops (shown
  arithmetic, S-1010 out-of-range flag + previous value, audit trail) mitigate but do **not**
  eliminate it. This is the safety floor the operator chose to lower (DL-035); the human must know
  it is now on the value they enter, not on a confirmation gate.
- **R2 — Gate-boolean trust boundary (ADR-7).** INV-2/Gate-1 trust a boolean the caller builds
  from live DB data. Keep building `Gate1Status` from the live factories; never cache a gate.
- **R3 — Patient model output cannot go live without a direct Python promotion** (no UI promote
  control). Safe today; if a promote control is added it is a **safety-design** change, not a
  wiring fix (DL-059).
- **R4 — The model is unproven on real data (DL-038 / OQ-8).** Revisit the whole design at ≥150
  real meals; on seed data it misses every low and Gate 1 correctly refuses to open.
- **R5 — Heed the S-1024 provenance banner.** Seeded/demo data renders like real capture except
  for that banner; it is the only on-screen signal that a shadow report or IOB is not hers.

## Watch next audit
- **First real-data window.** When ≥150 real meals exist, re-audit: hypo recall vs baseline on
  *her* data, the DL-042 calibration buckets against reality, the DL-047 90-day recency
  half-life, and the OQ-5 (80 mg/dL low line) false-alarm share in the 70–79 band. None is
  actionable now.
- **If a Gate-1 promotion UI is added**, audit it as a `[SAFETY]` story — it is the one screen
  where a human decides to put her in front of a model.
