# Remediation Brief 2026-07-17-01 — for SDET / Dev / BA

**From:** independent-auditor (outside your loop). **Audited commit:** `9198d10`.
**Full evidence:** `audits/AUDIT-2026-07-17-01.md` (read only if you want the proof).
**Overall audit verdict:** PASS.

## How to use this
Each item below is: what to change · where · why it matters for her · **Done when**
(the check that closes it). There is exactly one actionable item this round, and it is
process-integrity, not code. If any instruction seems to ask you to weaken a safety
invariant or a gate, STOP and escalate — that means I misread, not that the invariant
is wrong.

## Priority order
1. F1 (BA records the recurring test-ownership deviation; SDET adopts the added tests)

## For BA
### F1 — Record the recurring "Dev edited tests/" deviation (DL-017 recurrence)
- **Do:** Add a decision-log entry (next DL-0nn) naming the three GREEN commits below, each with an explicit decision (accept-with-reason, or re-author). Note that this is a **recurrence** of DL-017 and re-affirm the going-forward rule "Dev never touches `tests/`; each story's RED tests land in their own `test(S-nnn): RED` commit before any implementation." Correct any traceability prose that implies these three stories were strictly RED-first throughout.
- **Where:** `docs/decision-log.md`; the commits are
  `e746290` (S-601), `bc4f462` (S-602), `305f967` (S-603). Specifically:
  `e746290` removed an import, **weakened** `assert HYPO_STATES == frozenset({1,2})` to membership checks, and added `test_prob_at_least_above_top_state_is_zero`; `bc4f462` added `test_too_few_states_raises`; `305f967` added `test_insulin_col_out_of_range_raises` and `test_too_few_states_raises`.
- **Why (for her):** These are the guardrails against a model being quietly wrong about a low; when Dev quietly edits the tests that specify them and it goes unrecorded, the git history looks more disciplined than the process was — the exact "looks done" gap the audit exists to close. No safety invariant was weakened this round, but the pattern must not normalise.
- **Done when:** a DL entry exists citing all three SHAs with a per-commit decision and an explicit "recurrence of DL-017" note; and no traceability line asserts unqualified RED-first for S-601/602/603 without pointing at that DL entry.

## For SDET
### F1 (test-ownership half) — Adopt/harden the four Dev-added-or-weakened tests
- **Do:** Take ownership of the tests Dev added in the GREEN commits and decide whether to keep them as-is or strengthen them. In particular, restore an **exactness** assertion on the hypo set (e.g. `assert HYPO_STATES == frozenset({1, 2})`) so a future refactor cannot silently add state 4 or 5 to the up-weighted hypo set — the current membership-only check would not catch that.
- **Where:** `tests/unit/test_ordinal.py` (`test_hypo_weights_are_confidence_times_hypo_multiplier`, `test_prob_at_least_above_top_state_is_zero`), `tests/unit/test_brant.py::test_too_few_states_raises`, `tests/unit/test_bayesian_ordinal.py::test_insulin_col_out_of_range_raises` + `test_too_few_states_raises`.
- **Why (for her):** `HYPO_STATES` is what decides which glucose states get up-weighted precisely because they are the lows she cannot feel; the assertion that pins it must be tight and owned by the test author, not loosened by the implementer.
- **Done when:** the hypo-set assertion pins the exact frozenset again; the added edge tests are acknowledged as SDET-owned (comment/story note), and `pytest tests/unit/test_ordinal.py tests/unit/test_brant.py tests/unit/test_bayesian_ordinal.py` stays green.

## For Dev
Nothing this round. Do not "fix" the models — they pass every check (see "did NOT raise").

## Items I explicitly did NOT raise (so you don't invent work)
- **`method="bfgs"` instead of the spec's `lbfgs`** (`models/ordinal.py:136`) — correct and documented (DL-025); same MLE objective, forced by the pinned toolchain. Do not "restore" `lbfgs`; it re-breaks the build.
- **Absent-class handling** — `OrdinalFit.states` reporting the *observed* states and NOT fabricating a zero-probability column for an unseen hypo state is correct (DL-025). Do not "pad to 5 classes"; that would silently assert a low cannot happen. The 5-vector mapping + refusal belong to S-703/S-804.
- **Partial-PO fitter deferral** (DL-026) — correct. S-602 ships detection + a *raised* `ProportionalOddsViolation`; do not build a speculative partial-PO fitter before there is real data to reject on.
- **Frequentist ordinal has no INV-8 sign constraint** — correct. It predicts states, never a dose (ADR-10). INV-8 belongs on the OLS (`models/params.py`) and the Bayesian prior only; do not add a clamp to `fit_ordinal`.
- **Bayesian prior as sign constraint** — `HalfNormal` support ≥0 entering `eta` negatively is the intended INV-8 mechanism; the `inv8_beta_insulin_non_negative(min(draws))` assert-on-draws is the correct belt-and-braces. Do not replace it with a post-hoc clamp.
- **Models not wired to real meals / no gate evaluation yet** — correct for this epic. Do not wire them until the gates land.

## Watch next audit (heads-up, not work now)
- **O1 (carried):** the degenerate/absent-hypo-class case must surface as a **refusal or flag, not a crash and not a fabricated `P=0` column** once S-703/S-804 map the ordinal output to the 5-state readout. Verified not-yet-reachable this round (no wiring); check it the moment the mapping lands.
- **O2 (carried):** INV-7 must gate the row set fed to the ordinal/OLS once a model is wired to real meals. Verified not-yet-reachable this round (zero call sites outside tests). Check at first wiring.
- **EPIC 7 (the real test of this epic):** temporal CV must be re-run against the *actual* ordinal/Bayesian fit, reporting **hypo recall @ fixed FAR, calibration, Brier, Clarke/Parkes grid, off-by-one-state rate — NOT accuracy**. If any in-sample or CV score looks suspiciously high, treat it as leakage until the temporal-CV guards (`max(train.datetime) < min(test.datetime)`, no shared `date` across folds, scaler fit on train only) prove otherwise. S-701 leakage guards, then **S-703 gate enforcement (INV-1 `GateNotPassed`, no stubbed `icr`) must be merged and green before ANY EPIC 9 work.**
