# Remediation Brief 2026-07-17-02 — for SDET / Dev / BA

**From:** independent-auditor (outside your loop). **Audited commit:** `95244f1`.
**Full evidence:** `audits/AUDIT-2026-07-17-02.md` (read only if you want the proof).
**Overall audit verdict:** PASS.

## How to use this
Each item below is: what to change · where · why it matters for her · **Done when**
(the objective check that closes it). This was a clean epic — there is **one minor,
process-only item** and no code fix. If any instruction seems to ask you to weaken a
safety invariant or a gate, STOP and escalate — that means I misread, not that the
invariant is wrong. The prior audit's **F1 is fully resolved (both halves)** and does
**not** carry forward.

## Priority order
1. M1 (BA — commit-attribution hygiene note; optional-accept allowed)

## For BA
### M1 — Record commit-attribution hygiene for doc changes (lineage: DL-028 / DL-017)
- **Do:** In the next decision-log touch, add a short note that BA-owned doc changes
  (`docs/…`, decision log, traceability) should land in a `docs(...)` BA commit distinct
  from `test(...)`/`feat(...)` commits — **or** explicitly accept, with a one-line
  rationale, that DL-028 and the traceability edits were folded into the SDET commit
  `eec1e7f`. No git-history rewrite is needed or wanted.
- **Where:** `docs/decision-log.md`; the commit is `eec1e7f` (labeled `test(F1): SDET …`
  but also edited `docs/decision-log.md` +43 and `docs/traceability.md` +7).
- **Why (for her):** The audits reconstruct who-did-what from commit attribution to catch
  process erosion early; a fix for an ownership-blur that itself blurs ownership quietly
  weakens that signal. This is the same class of one-commit cross-owner edit DL-028 says
  to treat as a commit-time process failure — closing the loop keeps the history honest.
- **Done when:** a decision-log line references `eec1e7f` and either states the
  going-forward "docs land in `docs(...)` BA commits" rule or records explicit acceptance
  of the mixed commit. Substance of DL-028 itself is already correct — do **not** rewrite it.

## For SDET
Nothing this round. The exact-frozenset assertion is restored
(`tests/unit/test_ordinal.py:83`), the leakage/gate/metric tests are adversarial and
green, and I could not bypass the gate. Do not loosen anything.

## For Dev
Nothing this round. `prescribe/bolus.py` is the correct **gated refusal** entry point —
do **not** add dose arithmetic to it; that is S-901 and is separately human-gated (below).

## Items I explicitly did NOT raise (so you don't invent work)
- **`bolus.py` raising `NotImplementedError` when the gate opens** — correct. It proves the
  gate can open while guaranteeing no dose is fabricated before EPIC 9. Do **not** "finish"
  it into a calculator here.
- **`require_gate1` has no patient-visible caller yet** — correct. S-703 ships the
  enforcement primitive; S-804 (EPIC 8) wires it to the readout. Do not wire it early.
- **`temporal_cv` raising `TemporalLeakageError` (not `SafetyViolation`)** — correct and
  intentional (`models/validation.py` docstring): a leaky fold is a validation-integrity
  error, not one of INV-1..9. Do not route it through `core/safety`.
- **`models/metrics.py` exposing no `accuracy`/`accuracy_score`** — correct and guarded.
  Do not add a convenience accuracy metric.
- **Gate-status functions being plain pure functions with no caching** — correct (ADR-7).
  Do not add memoization/`lru_cache` "for performance"; it would let a stale "open" persist.
- **The high synthetic hypo-recall I measured (0.962)** — **not** a leakage finding; it
  matches in-sample (0.968), no train/test gap, and reflects trivially-separable synthetic
  data. Do not "fix" the CV runner.

## Watch next audit (heads-up, not work now)
- **EPIC 8 — output guardrails (S-801):** the five refusal conditions; diffuse posterior
  (max p=0.39 ⇒ "uncertain", 0.41 ⇒ a state); **baseline-vs-model conflict returns both
  with `conflict=true`, no winner picked**; predicted 601 raises / 600 does not (INV-6).
- **EPIC 8 — prediction log (S-802, INV-9):** write-before-return must be **enforced, not
  incidental** — the test mocks persistence to fail and asserts **no** prediction is
  returned. This is where a silent-low can still escape if a prediction is shown before it
  is logged.
- **EPIC 8 — kill switch (S-803):** trips on a bad run, suppresses output, shows baseline;
  a subsequent good prediction must **not** silently re-enable it.
- **EPIC 8 — patient risk readout (S-804, INV-2):** **hypo risk is the headline**, plain
  language, refusal is a *rendered state* never advice, no colour-only signalling; n=149 ⇒
  surface raises, no bypass. This is the first place INV-2 becomes patient-reachable —
  re-run the no-bypass checks against the real surface.
- **EPIC 9 — bolus calculator (S-901), may proceed now S-703 is green, but ALSO gated on a
  human:** it is **BLOCKED until the endocrinologist confirms ICR (OQ-1) and ISF (OQ-2)** —
  a human gate, not an agent decision; do not stub `icr`/`isf` to start. When it lands:
  no ML anywhere in the dose path (carbs/ICR + correction only), cap at 15 U **and** flag
  implausible inputs (INV-3), refuse at BG<80 (INV-4), never negative (INV-3), and the dose
  path stays behind `recommend_bolus`'s live Gate-2 check with no bypass.
