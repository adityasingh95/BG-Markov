# Remediation Brief 2026-07-17-03 — for SDET / Dev / BA

**From:** independent-auditor (outside your loop). **Audited commit:** `7dc93e2`.
**Full evidence:** `audits/AUDIT-2026-07-17-03.md` (read only if you want the proof).
**Overall audit verdict:** PASS. EPIC 8 is safe as built — INV-2 and INV-9 are genuinely unbypassable as I
tested them, the guardrails refuse rather than guess, the kill switch never self-re-arms, and no EPIC 9
dose arithmetic has crept in. The items below are minor doc/config hygiene only. Nothing here asks you to
weaken a gate or an invariant; if any item seems to, STOP and escalate — you have misread it.

## How to use this
Each item is: what to change · where · why it matters for her · **Done when** (the check that closes it).
Work top to bottom.

## Priority order
1. H1 (BA) — flip EPIC 8 story-doc statuses to Done
2. H2 (Dev) — put the INV-9 module under the coverage gate
3. H3 (BA) — settle the DL-029 story-doc attribution question
4. H4 (BA, optional) — record the mypy gate scope

## For BA

### H1 — Mark the EPIC 8 story docs Done
- **Do:** Change the `**Status:** In progress` header to a Done/Complete status in each of the five EPIC 8 story docs. The work is merged and green; the header is stale.
- **Where:** `docs/stories/S-801.md:4`, `docs/stories/S-802.md:3`, `docs/stories/S-803.md:3`, `docs/stories/S-804.md:4`, `docs/stories/S-805.md:4`.
- **Why (for her):** the story doc is the human "what was built" record; a story that reads "In progress" while shipped is the same narrative-vs-reality drift that lets an unfinished safety story look tracked.
- **Done when:** `grep -n "Status:" docs/stories/S-80*.md` shows none of the five saying "In progress"; the change lands in a `docs(...)` BA commit (per DL-029).

### H3 — Settle the DL-029 story-doc attribution question
- **Do:** Decide and record how BA-owned story docs (`docs/stories/*.md`) should be committed, because DL-029 (which you set in `ca44ef0`) says `docs/**` changes belong in a `docs(...)` BA commit, yet every EPIC 8 story doc was created inside an SDET `test(...)` RED commit. Pick one and write it as a DL entry: **(a)** amend DL-029 to explicitly exempt the story-restatement doc that ships with the RED commit (because it is part of establishing RED), or **(b)** require the story doc to land in its own `docs(...)` BA commit ahead of the RED test (CLAUDE.md TDD-loop step 1). The decision-log and traceability halves of DL-029 are already being followed correctly — this is only about `docs/stories/`.
- **Where:** `docs/decision-log.md` (new DL entry or amend DL-029). Evidence of the current pattern: `git show --name-only 7841427 d12a1da d892532 118a5c8 b60c635` (each `test(...)` SDET commit also creates a `docs/stories/S-80x.md`).
- **Why (for her):** the audits reconstruct who-did-what from commit labels; a rule the project sets and then does not follow erodes that attribution signal.
- **Done when:** `docs/decision-log.md` contains a dated decision naming which of (a)/(b) is the rule, committed as a lone `docs(...)` BA commit. If (b), the next story's doc appears in its own `docs(...)` commit before the RED test.

### H4 — (Optional) Record the mypy gate scope
- **Do:** Note in the decision log that `mypy --strict` is enforced over source packages only (`core api data cli features models prescribe`), not `tests/` or `alembic/versions/`, so "mypy clean" is unambiguous. `mypy --strict .` currently reports 11 errors in test/migration files (including 2 in `tests/safety/test_readout.py`); these are outside the gate by design.
- **Where:** `docs/decision-log.md`; scope is set by the CI step and `pyproject.toml [tool.mypy]`.
- **Why (for her):** prevents a future reader from assuming the whole tree type-checks when it does not.
- **Done when:** a DL entry states the mypy gate scope. (Purely documentary; no code change.)

## For Dev

### H2 — Put the INV-9 module under the coverage gate
- **Do:** Add `data/predictions.py` (or `data`) to the enforced coverage source so the write-before-return path is protected by the 90% gate. It is currently 100% covered by `tests/integration/test_prediction_log.py`, but the gate does not measure it, so a future regression on this path would pass CI. If you add the whole `data` package, exclude non-executable glue (migrations/`__init__`) via `[tool.coverage.report] exclude_also` so the gate stays meaningful.
- **Where:** `pyproject.toml:118` (`[tool.coverage.run] source`) and the CI `--cov=...` invocation in `.github/workflows/*.yml`.
- **Why (for her):** INV-9 (a prediction shown but not logged) is one of the most safety-critical paths; it must not be able to lose coverage silently.
- **Done when:** the CI coverage command includes `data/predictions.py` (or `data`), the gate still passes at ≥90%, and deliberately removing a line from `record_prediction`'s persist path makes the coverage gate fail (not just the unit test).

## For SDET
No action. The EPIC 8 test suite is strong: red-before-green held (every impl module was absent at its RED commit), no `feat` commit edited `tests/`, and the no-bypass / mocked-failure / boundary tests are real and adversarial. Do not "fix" the 2 mypy findings in `tests/safety/test_readout.py` by loosening anything — tests are outside the mypy gate; if you tidy them, keep the `# type: ignore` only where genuinely needed.

## Items I explicitly did NOT raise (so you don't invent work)
- **`prescribe/bolus.py` raising `NotImplementedError`** when Gate 2 is open is CORRECT — the dosing math is EPIC 9 and must not exist yet. Do not fill it in.
- **The trusted `Gate1Status.is_open` boolean** is the intended ADR-7 design (the invariant checks the boolean; `gate1_status()` computes it honestly). Do not "harden" `require_gate1` to recompute — that would duplicate gate logic and violate one-function-per-invariant.
- **INV-2/INV-9 enforcement via `core/safety.py`** is correct and not re-implemented in `readout.py`/`predictions.py`. Leave it.
- **The conflict path picking no winner (`state=None`)** and the hyper-state `hypo_risk="reduced"` framing are correct — hypo stays the headline. Do not add a tie-break.
- **`core/clock.py` using `datetime.now()`** is the sanctioned system clock for `logged_at` only (ADR-8), not a clinical timestamp. Leave it.
- **`data/` absent from the mypy scope** is not the issue; `data` IS in the mypy CI scope and clean. Only the *coverage* source omits it (H2).

## Watch next audit — EPIC 9 / S-901 (bolus calculator), and its human gate
Not actionable now; this is what the next audit will check when S-901 lands. Flagging so nothing is
started prematurely.
- **The human gate is necessary AND the code gate is not sufficient.** S-901 is BLOCKED until the endocrinologist confirms ICR (OQ-1) and ISF (OQ-2). A green `gate2_status(icr=...)` in code means the plumbing is open — it does NOT mean the clinical sign-off happened. The next audit will look for evidence (decision log / OQ list in `01-prd.md §9`) that OQ-1/OQ-2 were actually confirmed by a clinician, not a stubbed `icr = 8.3`. A fabricated or unilaterally-chosen ICR is a top-severity finding.
- **No ML in the dose path.** The calculator must be the clinical formula only (five lines), no model output feeding the dose.
- **INV-1/INV-3/INV-4 wired:** refuse at `BG < 80` (INV-4); cap at `MAX_BOLUS_U = 15` and additionally flag implausible input (e.g. `carbs = 900` typo) rather than silently dosing the cap; a negative computed dose ⇒ `0.0` (INV-3).
- **5 golden hand-computed doses** must exist as tests, and the dose must be **non-decreasing in carbs** and **non-increasing in IOB** (property tests).
- The next audit will also confirm the API/CLI callers build the gate statuses from live DB data via the honest factories (the trusted-boolean residual noted in this report).
