# Remediation Brief 2026-07-16-03 — for SDET / Dev / BA

**From:** independent-auditor (outside your loop). **Audited commit:** `461dbc5`
(EPIC 5 complete: S-501 baseline + shared binner, S-502 ISF derivation, S-503 ICR/ISF
constrained OLS — INV-8; plus verification of the 2026-07-16-02 remediation).
**Full evidence:** `audits/AUDIT-2026-07-16-03.md` (read only if you want the proof).
**Overall audit verdict:** PASS — **0 blocker · 0 major · 0 minor actionable.**

**There is no remediation work in this cycle.** The prior cycle's **A1** (features/ in the
coverage gate) and **A2** (record the `iob_at` clock-skew deviation, DL-023) are **both
genuinely resolved** — verified from the diffs and by running the coverage gate, not from commit
messages. EPIC 5 is TDD-honest, ruff/mypy/pytest/coverage all green (347 passed, 99.76% over
core+features+models), the INV-8 confounding-by-indication guard works under independent adversarial
re-derivation, and no forbidden pattern is present or unguarded.

## How to use this
This brief exists to tell you what is **done and must not be re-opened**, and what the next audit
will look at so you build it right the first time. If any bullet below seems to ask you to weaken a
safety invariant, a gate, or a leakage control, you have misread it — STOP and escalate.

## Priority order
No open items. Proceed to EPIC 6 (ordinal model) per the backlog. Read "Watch next audit" before you start.

## Items resolved this cycle (no action — confirmation only)
- **A1 (Dev/SDET) — RESOLVED.** `pyproject.toml` coverage source is now `["core","features","models"]`
  and CI runs `pytest --cov=core --cov=features --cov=models --cov-fail-under=90`; `features/cv.py`
  is now 100% (the prior uncovered `<n_splits+1`-days `ValueError` has a test). Do not shrink this
  source list; **add `prescribe/` to it when that package lands** (the pyproject comment already says so).
- **A2 (BA) — RESOLVED.** DL-023 records the deliberate `iob_at` clock-skew deviation from 07 §2 with
  the safe-direction rationale and "accept-as-is" resolution. Do not "correct" `iob_at` back to the
  §2 `0<age<td` filter without updating DL-023 and the `f(−5)=1` pinning test in the same change.

## Items I explicitly did NOT raise (do not "fix" these)
- **The INV-8 confounding guard is correct — do not touch it.** `fit_icr_isf` applies `β_carb≥0`,
  `β_ins≥0` via `scipy.lsq_linear` bounds; calls `inv8_beta_insulin_non_negative` (the single
  `core/safety.py` function) on the applied value; logs a prominent confounding-by-indication WARNING
  **before** constraining; and exposes `unconstrained_beta_ins` for **reporting only**. I reproduced
  all of this independently. Do not silence the warning, do not drop the unconstrained field, and do
  **not** feed `unconstrained_beta_ins` into any dose/prediction path.
- **The cross-check preference is correct.** On >20% disagreement, `cross_check_isf` returns the
  **correction-event** ISF (the unconfounded value wins). Do not flip this to prefer the OLS value.
- **The shared binner is correct and the tripwire is now armed.** `bg_to_state` boundaries
  `(54,80,181,251)` match 03 §; it is registered in `_BINNER_NAMES`; the baseline bins the
  **prediction**, never `pre_bg`. Do not add a `pre_bg` bin/state **input** column, and do not rename
  the binner without updating `_BINNER_NAMES`.
- **INV-6 wiring is correct.** `predict_baseline_bg` calls `inv6_predicted_bg_in_range` on the
  predicted BG; an out-of-[20,600] prediction raises rather than silently becoming State 5. Keep it.
- **ISF derivation gates are correct.** `derive_isf` applies only at ≥5 clean events, never silently
  swaps (returns both current and derived), and raises on ISF≤0; the repository bridge keeps only
  `food_in_window==False AND iob_at_start not None AND <0.5 AND bg_after present`. Do not relax the
  ≥5 gate, the NULL-IOB exclusion, or the ISF≤0 hard stop, and do not auto-apply a derived ISF to
  `patient_profile` (REQ-054 — applying is an explicit operator step).
- **`core/safety.py` is correct and untouched** (last changed S-104). Do not add project imports to
  it (ADR-6), do not add a `force=`/flag bypass, and do not re-implement any INV inside `models/`.

## Watch next audit (heads-up, not work now)
- **EPIC 6 ordinal model — the big one.** When `OrderedModel` lands: it must be `statsmodels`
  `OrderedModel`, **never** `LogisticRegression(multi_class=...)` (the `multi_class` grep must stay
  clean and biting); **no** per-state `for state in range(1,6): fit(...)` sub-model loop; predicted
  class probabilities must sum to 1; and **ordinal monotonicity** must hold —
  `P(state ≥ 4)` non-decreasing in `pre_bg`. `accuracy_score` must not be a reported metric.
- **EPIC 7 leakage + metric suite.** When the model is cross-validated: re-run the temporal-CV
  demonstration against the **actual** fit — scaler fit strictly on train folds, no `date` in both
  sides of any fold, `post_bg`/`elapsed_min` never in the feature vector. **If hypo recall or any
  score looks suspiciously good, assume leakage first.** Confirm the reported metric suite is hypo
  recall @ FAR / Brier / calibration / Clarke grid, not accuracy.
- **INV-7 in the OLS/model row set (EPIC 9 wiring).** When the ICR/ISF OLS or the ordinal model is
  wired to real meals, the row set must exclude hypo-rescued meals from the outcome regression while
  retaining them as hypo events (INV-7's function must gate the input). It has no call site yet (O2).
- **Degenerate ICR at wiring time (O1).** `fit_icr_isf.icr` can be ~0 when confounding clamps
  `β_ins` to 0; when this fit is wired into a recommendation, a degenerate ICR must surface as a
  refusal/flag, not a `carbs/icr` crash. Not reachable today (no consumer).
- **S-703 gate enforcement before EPIC 9.** No EPIC 9 (prescriptive) work may start before S-703 is
  merged and green; INV-1 must hard-block via `GateNotPassed`, never a stubbed `icr=8.3`. The next
  audit will check the build order holds and no gate is worked around.
