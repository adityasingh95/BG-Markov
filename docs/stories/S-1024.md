# S-1024 — Local deployment, and the database says what it is

**Epic:** 11 · **Safety story:** **Yes — provenance** · **Status:** ✅ Done (2026-07-31) ·
**Requirements:** REQ-056, `06 §7` · **Records:** DL-062

> The operator is about to run this on his laptop and click through it by hand. To do that
> usefully he needs demo data. **On screen, synthetic data is indistinguishable from hers.**

## Story
The app runs locally in one command, and **a database full of synthetic data says so on
every screen it renders.**

---

## Written safety argument

### Why a demo banner is a safety feature and not decoration

`scripts/seed_demo_db.py` produces 658 meals, 1034 boluses, 29 hypo rescues and a fitted
model. Every screen renders it exactly as it renders real capture: the adherence dashboard
says *"413 / 150 valid meals"*, the calculator subtracts a real-looking IOB, the shadow
report scores a real-looking model.

The failure this enables is not a crash. It is **believing a number**. An operator who
checks the shadow report on a seeded database and sees a plausible hypo recall has learned
nothing about her, and nothing on the page tells him so. That is the project's stated
worst case — *"a model that looks good on retrospective data, gets trusted, and is quietly
wrong about a low"* — reached by a different road.

### Why the marker lives in the database, not in the environment

An environment variable can be forgotten, inherited, or left set from the previous run, and
the mistake is silent in both directions. **The provenance belongs to the data**, so it is
written into the database by whatever created it and read back by whatever renders it. Move
the file, reopen it a month later, hand it to someone else — it still says what it is.

This is the same principle as `shadow_days` being a **derived count rather than a stored
boolean** (S-1007) and gates being evaluated live (ADR-7): the answer is re-derived from
the thing itself, so it cannot drift from reality.

### Why one banner, in one place

Two `Jinja2Templates` instances already exist — `api/app.py` and `api/bolus.py`. A context
added to one and not the other is exactly the shape of every gap this epic found. So they
are consolidated into a single `api/templating.py`, the banner is a **context processor** on
that one object, and a test enumerates **every** HTML route and asserts the banner on all of
them. Per-template opt-in would be forgotten on the next page anyone adds.

## Acceptance criteria

1. `./scripts/dev.sh setup` builds a venv, installs, and creates the schema **via Alembic**.
2. `./scripts/dev.sh serve` runs the app on `127.0.0.1:8000` (`06 §7` — never off localhost).
3. `./scripts/dev.sh demo` creates a **seeded, demo-marked** database and serves it.
4. `./scripts/dev.sh reset` deletes the local database, with a confirmation.
5. A demo-marked database renders a banner on **every** HTML page.
6. An unmarked database renders **no** banner on any page.
7. `seed_demo_db` **always** marks. A seeder that can produce an unmarked demo database is
   the whole defect.
8. `docs/RUNBOOK.md`: how to run it, what to click, and **what is known not to work**.

## TDD strategy (SDET first — written and seen to fail)

`tests/unit/test_provenance.py` — mark, read back, idempotent re-mark, unmarked ⇒ `False`.

`tests/safety/test_demo_banner.py`
- **★ Every HTML GET route** — enumerated from the app, not hand-listed — shows the banner
  on a demo-marked database. A hand-listed set is a list someone forgets to extend.
- **★ No page shows it on an unmarked database.** A banner that is always on is a banner
  nobody reads, and it would then be present on her real records.
- **★ There is exactly ONE `Jinja2Templates` instance under `api/`.** The second instance is
  how the first gap gets in.
- **★ `seed_demo_db.seed()` produces a marked database.** The seeder is the only thing that
  makes synthetic data, so it is the only place the mark can be forgotten.

**Adversarial angle.** (a) **Reading the flag from an env var** — silently wrong in both
directions. (b) Adding the banner to `index.html` only, which is the page you happen to be
looking at. (c) A banner that renders as an empty string when unmarked but still occupies
layout, so the "no banner" test passes on a page that has one.

---

## Outcome (BA, 2026-07-31)

**Done.** `data_provenance` (migration `d3a7c1f8b204`), `data/provenance.py`,
`api/templating.py` as the single templates object, `scripts/dev.sh`, `docs/RUNBOOK.md`.
Records **DL-062**.

```
./scripts/dev.sh setup | serve | demo | status | reset | check
```

Verified against a real seeded database: `/`, `/bolus`, `/operator`, `/operator/shadow`,
`/basal`, `/corrections` all render the banner; an unmarked database renders none.

### ★ Found while making the tests pass — the banner read the wrong database
The first implementation opened its **own** session from `api.deps`, so it described
whatever `BGAPP_DB_URL` pointed at rather than what the page was rendering. **That is worse
than no banner: it is confidently mislabelled.** It now reads a flag set per request by
`provenance_middleware`, which resolves the session through `app.dependency_overrides`
exactly as a route dependency does — so the banner and the page cannot disagree about which
file they are looking at.

The test caught it because it pointed a route at one database; a test that had used the
same default file for both would have passed and proved nothing.

### Guards seen to fire

| Plant | Caught by |
|---|---|
| the seeder forgets to mark | `test_the_seeder_always_marks_what_it_makes` |
| a second `Jinja2Templates` in `bolus.py` | the structural guard **and** the `/bolus` page test — both halves, which is the point of having both |

### ★ The first real run failed, and it was my fault
`dev.sh migrate` shelled out to the **`sqlite3` CLI** to ask whether a database had migration
history. That binary is not installed here. The check silently answered *"no tables"*, the
stamp never ran, and `alembic upgrade` died with `table audit_log already exists` and forty
lines of traceback — **the operator's first command failing with a wall of SQL**, which is
the exact experience this runbook exists to prevent.

The fix is structural, not a patch: **bash orchestrates, Python does the work**
(`scripts/migrate_db.py`). A shell script that reasons about a database depends on whatever
binaries happen to be on the machine, and fails in a way that reads as a broken migration
rather than a missing tool — sending the reader to look in entirely the wrong place. The
venv's Python was already a hard requirement; `sqlite3` never was.

`tests/integration/test_ensure_schema.py` covers the three cases and adds a guard that
`dev.sh` never shells out to `sqlite3` again.

### ★ Two more found by asking "would this run on his laptop?"
This container has **bash 5.2**; a stock macOS has **bash 3.2**, frozen in 2007.

1. **`cmd_reset` used a local array.** In bash < 4.4, `${arr[@]}` on an *empty* array is an
   unbound-variable error under `set -u` — so `reset` would abort on a laptop with nothing to
   delete, which is the one case where it should say *"nothing to delete"* calmly. Rewritten
   without arrays.
2. **There was no way to find out what was missing.** Added `dev.sh doctor`: shell, Python,
   venv, deps, port — each reported in a sentence, with the install command for macOS,
   Ubuntu and python.org. It immediately caught a port already in use on the first run.

`tests/integration/test_dev_script_portability.py` guards the class: no `sqlite3`, no
bash-4-only constructs, no GNU-only `sed -i`, and `doctor` must exist and be reachable. Each
guard names the construct **and why it breaks**, because a style rule nobody understands gets
deleted.

### The package
`start.sh` + `START-HERE.md`: preflight, install, seed, serve — one command. Built with
`git archive`, so only tracked files ship: no `.venv`, no `.git`, no caches, **no database**.
800 KB.

**Verified by extracting the zip into an empty directory on a machine with no venv and
running `./start.sh`**: doctor → venv → deps → schema → seed (658 meals) → serve, and all
seven HTML pages returned 200 with the DEMO DATA banner. That is the only test of a package
that means anything.

### The alembic/create_all trap, handled rather than documented
`api/deps.py` calls `create_all()` on the first request, so a database created by simply
starting the app has every table and **no `alembic_version`**. `alembic upgrade` then fails
with *"table already exists"* and reads as a broken migration. `dev.sh migrate` stamps first:
the schema **is** at head, it just was not recorded as such.

### Still open
- `bgapp-dev.db` is git-ignored because it would be **her records**. There is no
  encryption-at-rest and no backup schedule wired into `dev.sh`; `cli backup` exists
  (S-304) and running it is currently a manual act.
- The banner is not shown on `/openapi.json` or any JSON response. API clients get no
  provenance signal. Not needed for manual verification; worth knowing if anything ever
  reads this app programmatically.
